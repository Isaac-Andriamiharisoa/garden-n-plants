<h1>
    All the plants
</h1>

<ul>
<% plants.each do |plant| %>
    <li>
        <%= plant.name %>
        <%= link_to 'see plant', plant_path(id: plant.id) %>
    </li>
<% end %>

<%= link_to 'New plant', new_plant_path %>
</ul>
